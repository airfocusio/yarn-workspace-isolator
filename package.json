{
  "name": "yarn-workspace-isolator",
  "version": "0.1.0-rc1",
  "license": "MIT",
  "author": {
    "name": "Christian Hoffmeister",
    "email": "mail@choffmeister.de"
  },
  "bin": {
    "isolate-workspace": "./bin/isolate-workspace"
  },
  "scripts": {
    "lint-fix": "yarn prettier --write",
    "lint": "yarn prettier --list-different",
    "prettier": "prettier 'bin/*' 'src/**/*.js'",
    "test": "yarn lint"
  },
  "dependencies": {
    "copy-dir": "^0.3.0",
    "lodash": "^4.17.13",
    "yargs": "^11.0.0"
  },
  "devDependencies": {
    "prettier": "^1.11.1"
  },
  "engines": {
    "node": ">=8.0.0",
    "yarn": ">=1.5.0"
  }
}
